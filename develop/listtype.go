package main

// MYTYPE is replaced by {{.MyType}} when generating
// the template 'template.go' from 'list.go'.
// But the type declaration itself is not in the template
// so it is in this file
type MYTYPE = int
